@AGENTS.md

# Claude Code

- Use the project skill at `.claude/skills/cactus-product-context/SKILL.md`.
- Invoke it explicitly with `/cactus-product-context` for product decisions, PRDs,
  implementation plans, analytics specs, and roadmap questions.
- Run `/skills` if the skill is not visible.
- Keep this file concise; load detailed evidence through the skill's references only
  when needed.
